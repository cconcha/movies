import styled from 'styled-components'
import { space, color, layout, grid, background, border, position, shadow, flexbox, typography } from 'styled-system'

const Box = styled.div`
  ${space} ${color} ${layout} ${grid} ${background} ${border} ${position} ${shadow} ${flexbox} ${typography}
`

export default Box
